Task upload
#Set the path of local jar file
LocalJarPath SparkTest.jar
#Set the relative path of jar file which is uploaded to Ubuntu master host. 
RemoteJarPath SparkTest.jar
#Indicate if delete the existing jar file on remote Ubuntu directory. 
DeleteExistingFile true